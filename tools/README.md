# Tools

Repo-wide tooling that supports the migration journey but isn't tied to a single phase.

## Status

🚧 **Placeholder.** Content lands here in **Phase 2** of the consolidation rollout. See [`docs/internal/`](../docs/internal/) for sequencing.

## What will live here

- `model-migration-eval/` — Flask web UI for side-by-side model comparison with multi-user auth, Bicep IaC, and `azd up` deployment. A visual companion to the `MigrationEvaluator` SDK in [`../04-evaluate/src/evaluate/`](../04-evaluate/).
